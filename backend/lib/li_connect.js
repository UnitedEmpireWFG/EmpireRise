export async function sendLinkedInConnect(job) {
  // Use your stored OAuth token and call your LinkedIn helper
  // Include job.note when available
  // Return { ok: true } on success or { ok: false, error: 'rate_limited' }
  return { ok: true }
}
