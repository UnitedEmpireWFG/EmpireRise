{
  "name": "empire-rise",
  "private": true,
  "type": "module",
  "version": "1.0.0",
  "scripts": {
    "backend": "node backend/server.js",
    "lint": "npm --prefix backend run lint"
  },
  "dependencies": {
    "concurrently": "^8.2.2",
    "multer": "^2.0.2"
  }
}
